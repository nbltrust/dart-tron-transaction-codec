name: tron_codec_test
version: 0.1.0
publish_to: 'none'

dependencies:
  eth_abi_codec:
    git:
     url: https://github.com/nbltrust/dart-eth-abi-codec
     ref: 2.0.0

  tron_codec:
    path: ../

dev_dependencies: 
  meta: any
  test: any

environment:
  sdk: ">=2.12.0 <3.0.0"
